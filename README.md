# generic_sso
Drupal authentication module derived from git.drupalcode.org/project/ldap_sso but without the LDAP dependency, permitting use with any authentication backend that exposes a SERVER variable.
